 //Escreva um programa que exiba apenas os números pares de 1 a 50.

let contadora = 1
do {
    if(contadora %2 == 0) {
        console.log(contadora)
    }
    contadora++
} while (contadora <51);